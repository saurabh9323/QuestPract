import type {ConceptAdventure} from './concept-adventures';
const pg='https://www.postgresql.org/docs/current/';
function lesson(id:string,title:string,world:string,emoji:string,when:string,trap:string,steps:[string,string][],examples:Record<string,string>,challenge:string,answer:string,source?:string):ConceptAdventure{return {id,title,world,emoji,when,trap,steps,examples,challenge,answer,hints:['State the requirement and the boundary first.','Walk through the example, including one failure case.'],source}}
export const foundationLessons:ConceptAdventure[]=[
 lesson('hld','HLD: draw the city before the buildings','Architecture','🏙️','High-level design describes major components, their responsibilities, data flow, capacity and reliability choices. Use it to agree on system boundaries before implementation.','HLD is not a collection of fashionable infrastructure boxes. Start with requirements and justify every extra component.',[['Requirements','For a booking service: avoid double booking, show availability, and define expected traffic and latency.'],['Components','Client → API → booking service → PostgreSQL. Add a background worker for email if synchronous delivery is unnecessary.'],['Data flow','Read availability, attempt a booking transaction, return its result, then deliver the notification.'],['Failure review','Discuss timeouts, retry semantics, duplicate requests, ownership checks, and monitoring.']],{Design:'Client → API → Booking service → PostgreSQL\n                     ↓\n                 Outbox → Worker → Email\n\nCapacity questions: peak requests/s? retention? hot events?\nCorrectness: one confirmed booking per seat.'},'When would you add a cache, and what would you keep authoritative?','Add caching for measured read pressure with a defined freshness policy. The database remains authoritative for booking allocation; stale cached availability cannot authorize a sale.'),
 lesson('lld','LLD: design the rooms and their rules','Architecture','🧱','Low-level design turns a component into implementable interfaces, functions/classes, schemas, state transitions, error behavior and algorithms. Use it before coding tricky interactions.','LLD does not require OOP. A functional module with clear inputs, outputs and invariants is also detailed design.',[['Interface','Define reserve(seatId, userId, requestKey) and its success/error result.'],['State','Model available → held → confirmed, including hold expiry and cancellation.'],['Implementation','Specify validation, repository contract, transaction boundaries and atomic uniqueness.'],['Verification','Cover duplicate retries, expired holds, authorization and concurrent requests.']],{TypeScript:'type Result =\n | {ok:true; bookingId:string}\n | {ok:false; reason:"unavailable"|"invalid"};\ninterface BookingRepository {\n reserve(seatId:string,userId:string,key:string):Promise<Result>;\n}',Python:'def reserve(repository, seat_id, user_id, key):\n    validate(seat_id, user_id, key)\n    return repository.reserve_atomically(seat_id, user_id, key)'},'What is the difference between HLD and LLD for the same booking feature?','HLD explains services, storage, data flow and system-wide tradeoffs. LLD defines the reserve contract, data representation, transitions and exact failure handling inside those components. Move between both levels as constraints become clearer.'),
 lesson('linux-basics','Linux: meet the operating system','Linux','🐧','Linux is a kernel used by operating-system distributions such as Ubuntu. Developers encounter it in cloud servers, containers and CI runners.','Linux, a distribution, Bash, and a terminal are different things. Containers share a host kernel; they do not necessarily run a complete separate OS.',[['Terminal','The terminal accepts text and displays output.'],['Shell','Bash or another shell parses a command and launches programs.'],['Kernel','The kernel schedules processes and manages memory, files and devices.'],['Result','A program returns output and an exit status to its parent.']],{Shell:'pwd          # current directory\nwhoami       # current user\nuname -s     # kernel name\ncat /etc/os-release  # distribution information'},'Is Bash the same as Linux?','No. Bash is a shell that can run on several operating systems. Linux is the kernel; a distribution adds user-space tools, services and package management.', 'https://docs.kernel.org/'),
 lesson('linux-files','Linux: navigate a project safely','Linux','🗂️','Use paths and file-inspection commands to understand deployments and logs before changing anything.','A relative path depends on the current directory. Quote paths containing spaces. ls does not recursively read file contents.',[['Locate','pwd shows your current directory.'],['List','ls -la lists entries including hidden files and metadata.'],['Navigate','cd moves the shell to a directory; .. means the parent.'],['Inspect','cat reads a small file; less pages through a larger one.']],{Shell:'pwd\nls -la\ncd /srv/app\ncat package.json\nless app.log\n# mkdir, cp, mv and rm modify files; know the target first.'},'What does cd .. change, and why might a relative path stop working afterward?','It changes the shell’s working directory to its parent. Relative paths are resolved from that new directory, while an absolute path begins at the filesystem root.', 'https://www.gnu.org/software/coreutils/manual/coreutils.html'),
 lesson('linux-pipes','Linux: connect command streams','Linux','🚰','Pipes compose tools for tasks such as finding errors and counting matching lines.','A pipe passes stdout, not stderr by default. > truncates an output file; >> appends. Shell quoting changes how arguments are parsed.',[['Read','A command produces a stream of text.'],['Filter','grep selects matching lines.'],['Transform','A downstream tool such as wc counts the filtered lines.'],['Status','Check exit statuses as well as visible output, especially in automation.']],{Shell:"grep 'ERROR' app.log | wc -l\ntail -n 20 app.log\n# In Bash scripts: set -o pipefail\n# helps expose failures from earlier pipeline commands."},'Why can the final command succeed even if an earlier pipeline command failed?','Without pipefail, a Bash pipeline normally reports the exit status of its final command. A downstream command can successfully process an empty stream even if the first command failed.'),
 lesson('linux-permissions','Linux: who may read, write, or execute?','Linux','🔑','Permissions control owner, group, and other access to files and directories. Inspect them when deployment scripts or applications cannot access a resource.','chmod 777 is not a general fix. For directories, execute means traversal and read means listing; deleting entries depends on directory permissions.',[['Identity','Find which user and group the process runs as.'],['Metadata','Inspect owner and rwx permission bits with ls -l.'],['Least access','Grant only the capability the intended user/group needs.'],['Recheck','Run the operation under the actual application identity.']],{Shell:'ls -l deploy.sh\nchmod u+x deploy.sh   # add owner execute permission\n# 640: owner read/write, group read, others none\n# sudo runs with elevated privilege; it is not always required.'},'What does permission 640 mean for a regular file?','Owner: read and write (6). Group: read (4). Others: no access (0). It does not grant execute permission.', 'https://www.gnu.org/software/coreutils/manual/html_node/Mode-Structure.html'),
 lesson('linux-processes','Linux: investigate a slow service','Linux','🔍','Inspect process state, memory, listening sockets and logs to diagnose a service.','Commands vary by distribution. systemctl and journalctl apply to systemd systems; a container may not have them. Do not begin by killing random processes.',[['Process','ps or top identifies running processes and resource usage.'],['Port','ss shows listening sockets; verify that the service bound the expected address.'],['Logs','Read application logs or systemd journal entries where applicable.'],['Hypothesis','Separate CPU pressure, memory pressure, blocked I/O, and configuration failures.']],{Shell:'ps -ef\ntop\nfree -h\ndf -h\nss -ltn\njournalctl -u myapp --since "10 minutes ago"\n# Last command requires a matching systemd service.'},'Does high response latency always mean the CPU is busy?','No. A process can wait for a database, network, disk, lock, or worker slot while consuming little CPU. Correlate latency with resource metrics and request traces.'),
 lesson('cpu','CPU: the general-purpose coordinator','Hardware','🧠','A central processing unit runs operating-system and application instructions. CPUs handle diverse control flow and many kinds of work.','More cores do not automatically accelerate one sequential task. Waiting on a database is not fixed by adding CPU cores.',[['Instructions','A running program requests arithmetic, branching and memory access.'],['Execution','CPU cores execute instructions; caches help access frequently used data.'],['Scheduling','The OS schedules runnable threads onto available cores.'],['Bottleneck','Measure compute, memory and I/O before choosing a larger machine.']],{JavaScript:'// Repeated CPU-heavy work can block a JS event loop.\n// For suitable workloads, use a worker or job queue.\n// Network requests waiting for responses are a different bottleneck.'},'Why might doubling cores leave a single sequential task unchanged?','Its next operation depends on the previous one, so there is no independent work to spread across those cores. Parallelism and coordination overhead determine the benefit.', 'https://aws.amazon.com/compare/the-difference-between-gpus-cpus/'),
 lesson('gpu','GPU: the parallel calculation workshop','Hardware','🎨','Graphics processing units are useful for suitable parallel numeric workloads such as image processing and machine learning. They work alongside CPUs.','A GPU is not universally faster. Transfer overhead, batching, branching, precision and memory capacity matter.',[['Prepare','CPU prepares input and schedules a GPU operation.'],['Transfer','Data may need to move to device memory.'],['Parallel work','Many similar operations run across execution resources.'],['Return','Synchronize and retrieve results when required.']],{Python:'# Conceptual pipeline, not a specific GPU library:\n# batch = prepare_inputs()\n# device_batch = transfer(batch)\n# result = parallel_kernel(device_batch)\n# output = retrieve(result)'},'Why could a tiny calculation be slower on a GPU?','Setup, transfer and synchronization can cost more than the calculation saves. Measure the complete workload rather than only the device kernel.', 'https://aws.amazon.com/compare/the-difference-between-gpus-cpus/'),
 lesson('memory-storage','RAM, disk, and caches: different shelves','Hardware','🗄️','Distinguish working memory, persistent storage and reusable cached data when reasoning about performance or data loss.','“Cached” does not mean durable or always correct. A process crash can remove in-memory state while persisted data remains.',[['Storage','Durable files or database pages live on persistent media.'],['Memory','Active data is loaded into RAM for processing.'],['Cache','A reusable copy can avoid slower repeated work.'],['Lifecycle','Define eviction, invalidation and persistence independently.']],{Design:'Database record → application memory → response cache\nAsk: authoritative owner? expiry? restart behavior?\nA Redis deployment’s durability depends on its configuration.'},'Why should an in-memory shopping cart not be your only durable order record?','A process restart, eviction or another server instance may lose or not see that state. Durable orders need a persistence strategy consistent with the required guarantees.'),
 lesson('pg-types','PostgreSQL: choose the right data type','PostgreSQL','🧬','Match database representation to meaning: integer counts, exact decimal amounts, text, Boolean flags, timestamps, UUIDs or structured JSON.','jsonb is useful for variable attributes but does not replace relational constraints. timestamptz represents an instant; it does not preserve the original named timezone.',[['Meaning','Identify what a value represents and what operations it needs.'],['Type','Choose numeric for exact decimals, integer for bounded counts, and text for strings.'],['Time','Store an instant using timestamptz; keep a separate timezone when business rules need it.'],['Schema','Use columns and relations for stable queryable structure; jsonb for suitable flexible attributes.']],{SQL:'CREATE TABLE catalog_item (\n id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n name text NOT NULL,\n price numeric(12,2) NOT NULL,\n active boolean NOT NULL DEFAULT true,\n created_at timestamptz NOT NULL DEFAULT now(),\n attributes jsonb NOT NULL DEFAULT \'{}\'::jsonb\n);'},'Would double precision be your first choice for exact currency totals?','Usually no: binary floating-point cannot represent every decimal fraction exactly. Choose numeric with an appropriate precision/scale or a carefully defined integer minor-unit representation.',pg+'datatype.html'),
 lesson('pg-constraints','PostgreSQL: put invariants in the database','PostgreSQL','🛡️','Use NOT NULL, CHECK, UNIQUE, PRIMARY KEY and FOREIGN KEY to enforce valid states across all writers.','A CHECK expression permits NULL unless the expression is false; pair it with NOT NULL when required. Uniqueness and referential integrity solve different problems.',[['Required','NOT NULL prevents a missing required value.'],['Valid range','CHECK rejects a value outside a permitted condition.'],['Identity','PRIMARY KEY identifies rows; UNIQUE prevents duplicate constrained values.'],['Relationship','FOREIGN KEY requires a matching referenced key when non-null.']],{SQL:'CREATE TABLE lesson_attempt (\n id integer PRIMARY KEY,\n user_id integer NOT NULL REFERENCES learner(id),\n answer text NOT NULL,\n request_key text NOT NULL,\n minutes integer NOT NULL CHECK (minutes >= 0),\n UNIQUE (user_id, request_key)\n);'},'Is CHECK (minutes >= 0) enough to reject NULL minutes?','No. Add NOT NULL if minutes must always have a value. CHECK handles the permitted range.',pg+'ddl-constraints.html'),
 lesson('pg-relations','PostgreSQL: connect rows with foreign keys','PostgreSQL','🔗','Represent relationships such as one user owning many attempts using a stable referenced key.','A foreign key does not implement authorization. Deletion behavior must be chosen deliberately; cascade can remove dependent rows.',[['Parent','learner.id is a unique primary key.'],['Child','attempt.user_id references a learner.'],['Join','Join on the key to retrieve related data.'],['Delete policy','Choose restrict/no action, cascade or another policy according to ownership and retention.']],{SQL:'SELECT a.id, u.name\nFROM attempt AS a\nJOIN learner AS u ON u.id = a.user_id;\n-- The referencing user_id may need its own index for common queries.'},'Why might deleting a learner be rejected?','Existing attempts can still reference it. The configured foreign-key action determines whether deletion is rejected or related rows are changed; do not assume cascade.',pg+'ddl-constraints.html'),
 lesson('pg-transactions','PostgreSQL: ACID and concurrent work','PostgreSQL','🤝','Transactions group related changes. ACID means atomicity, consistency, isolation and durability; isolation controls what concurrent transactions can observe.','A transaction alone does not prevent every race. PostgreSQL’s default Read Committed gives each statement a fresh snapshot; choose locking or stronger isolation when needed.',[['Begin','Start the unit of work.'],['Modify','Apply updates while constraints protect declared invariants.'],['Commit or rollback','Commit the unit or roll it back after a failure.'],['Concurrency','Consider competing transactions and retry relevant serialization failures.']],{SQL:'BEGIN;\nUPDATE stock SET quantity = quantity - 1\nWHERE sku = \'BOOK\' AND quantity > 0\nRETURNING quantity;\n-- Proceed only when a row was returned.\nCOMMIT;'},'Why is SELECT quantity followed by an unconditional UPDATE risky?','Another transaction can change the quantity between the read and write. An atomic guarded update or appropriate locking expresses the condition with the modification.',pg+'transaction-iso.html'),
 lesson('pg-indexes','PostgreSQL: an index is a route, not free speed','PostgreSQL','🧭','Indexes can reduce work for selective filters, joins or ordering. Choose them from real query patterns and inspect the plan.','Indexes consume storage and add write overhead. The planner may prefer a sequential scan; EXPLAIN ANALYZE actually executes the statement.',[['Query','Identify frequent predicates and order requirements.'],['Candidate index','Choose columns and order that fit the access path.'],['Plan','Inspect estimates and, when safe, actual execution statistics.'],['Tradeoff','Measure read benefit against write and storage cost.']],{SQL:'CREATE INDEX attempt_user_time\nON attempt (user_id, created_at DESC);\nEXPLAIN SELECT * FROM attempt\nWHERE user_id = 42\nORDER BY created_at DESC LIMIT 10;'},'Why does adding an index to every column hurt?','Writes must maintain each applicable index and storage grows. Many indexes may not match useful access paths, so measure representative queries.',pg+'indexes.html'),
 lesson('sql-flow','SQL: WHERE versus HAVING','PostgreSQL','🚿','WHERE filters individual rows before aggregation; HAVING filters groups afterward. This is logical query reasoning, not a fixed physical execution plan.','NULL uses three-valued logic: use IS NULL rather than = NULL. A filter in WHERE can change a LEFT JOIN’s intended behavior.',[['Rows','FROM/JOIN builds the source relation.'],['WHERE','Keep matching rows, such as paid orders.'],['GROUP BY','Group by user and aggregate totals.'],['HAVING','Keep only groups whose total passes the threshold.']],{SQL:'SELECT user_id, SUM(amount) AS total\nFROM orders\nWHERE status = \'paid\'\nGROUP BY user_id\nHAVING SUM(amount) >= 100\nORDER BY total DESC;'},'Can WHERE SUM(amount) > 100 replace HAVING here?','No. WHERE operates before this grouping step; the grouped sum is evaluated for HAVING. A subquery can expose a previously calculated total to an outer WHERE.'),
 lesson('docker-foundations','Docker: package the app, run a container','Delivery','🐳','A container image packages an application’s runtime filesystem and configuration. Containers make a deployment environment more reproducible.','An image is not a running container. Data in a container’s writable layer is not a durable database strategy, and secrets should not be baked into images.',[['Build','Create an image from declared instructions and dependencies.'],['Run','Start a container with its own process environment.'],['Configure','Provide environment-specific configuration and runtime secrets.'],['Persist','Use appropriate volumes or external services for durable data.']],{Dockerfile:'# Illustration: choose a maintained runtime image for your app.\nFROM node:22-alpine\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci --omit=dev\nCOPY . .\nCMD ["node", "server.js"]\n# Requires server.js and a .dockerignore; adapt for build steps.'},'Why does “works in Docker” not prove a deployment is reliable?','Networking, permissions, secrets, storage, resource limits, health checks and rollback still need correct production configuration.'),
 lesson('cicd-foundations','CI/CD: move a change through gates','Delivery','🚚','Continuous integration validates frequent changes; delivery keeps deployable artifacts ready, while deployment can automate their release.','Passing a pipeline is evidence for its checks, not proof that no defects exist. Production rollout and rollback deserve explicit design.',[['Change','A commit enters version control and review.'],['Validate','Run relevant static checks and behavioral verification.'],['Artifact','Produce a versioned artifact tied to the commit.'],['Release','Deploy with health signals, staged rollout where suitable, and rollback.']],{Design:'Commit → Review → Checks → Versioned artifact\n                               ↓\n                         Staging → Release\n                               ↑        ↓\n                             Rollback / signals'},'Why deploy the same built artifact to staging and production?','Rebuilding can introduce different dependencies or outputs. Promoting a versioned artifact reduces that source of drift, while environment configuration is managed separately.'),
 lesson('rag-foundations','RAG: bring evidence before answering','AI foundations','📚','Retrieval-augmented generation retrieves relevant documents and supplies them as context to a model. Use it for answers grounded in a controlled knowledge collection.','Retrieval can fail and generated text can still be wrong. Apply access controls before retrieval and treat retrieved text as data rather than instructions.',[['Ingest','Parse, chunk and index permitted documents with metadata.'],['Retrieve','Search for relevant evidence using the user’s query and authorization scope.'],['Generate','Ask the model to answer from the retrieved evidence and identify sources.'],['Evaluate','Check relevance, faithfulness, missing evidence, latency and cost.']],{Python:'# Adapter sketch, not a model API:\ndocs = search(query, allowed_document_ids)\nif not docs:\n    return "Insufficient evidence"\nreturn answer_with_citations(query, docs)'},'How is RAG different from fine-tuning?','RAG supplies selected information at request time; fine-tuning changes model parameters through training. Neither automatically guarantees factual or authorized answers.'),
 lesson('observability','Logs, metrics, traces: follow the incident','Delivery','🔭','Use logs for events, metrics for aggregate trends, and traces for a request’s path through components.','Collecting everything is not the goal. Avoid logging credentials or personal data unnecessarily, and control high-cardinality labels.',[['Signal','Latency or error-rate metrics reveal a user-visible problem.'],['Trace','Follow a slow request across API, service and database spans.'],['Logs','Read relevant event detail with a correlation identifier.'],['Action','Mitigate, verify recovery, and record the root cause and prevention.']],{Design:'Request ID → API span → service span → database span\nMetrics: request rate, errors, p95 latency\nLogs: structured events with safe context'},'Why is average latency alone insufficient?','It can hide a slow tail. Percentiles and distributions reveal users experiencing severe latency even when the average looks acceptable.'),
];
