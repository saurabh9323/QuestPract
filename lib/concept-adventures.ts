export type ConceptAdventure={id:string;title:string;world:string;emoji:string;when:string;trap:string;steps:[string,string][];examples:Record<string,string>;challenge:string;hints:string[];answer:string;source?:string};
export const conceptAdventures:ConceptAdventure[]=[
 {id:'encapsulation',title:'Encapsulation: protect the treasure',world:'OOP',emoji:'🔐',when:'Keep state valid by making changes pass through controlled operations.',trap:'A private field is a language boundary, not authorization or encryption. Server permission checks still matter.',steps:[['Caller','The caller requests a deposit of 10.'],['Public method','deposit(amount) validates a finite positive amount.'],['Private balance','Only the class’s method changes #balance.'],['Read-only result','The caller reads the balance through a getter.']],examples:{JavaScript:'class Wallet {\n #balance = 0;\n deposit(n) {\n  if (!Number.isFinite(n) || n <= 0) throw Error("Invalid");\n  this.#balance += n;\n }\n get balance() { return this.#balance; }\n}\nconst w = new Wallet(); w.deposit(10); // w.balance: 10',Python:'class Wallet:\n    def __init__(self): self.__balance = 0\n    def deposit(self, amount):\n        if amount <= 0: raise ValueError("Invalid")\n        self.__balance += amount\n    @property\n    def balance(self): return self.__balance\n# Name mangling discourages access; it is not security.',CSharp:'class Wallet {\n public decimal Balance { get; private set; }\n public void Deposit(decimal amount) {\n  if(amount <= 0) throw new ArgumentException();\n  Balance += amount;\n }\n}'},challenge:'Why is exposing a writable balance risky? How would you implement withdrawal?',hints:['Name the invariant first.','Every mutation must preserve a nonnegative balance.'],answer:'A writable field allows invalid values and bypasses checks. A withdrawal method validates the amount and funds before changing balance. Concurrent database withdrawals also need atomicity; a class alone does not prevent races.'},
 {id:'polymorphism',title:'Polymorphism: one command, many messengers',world:'OOP',emoji:'📮',when:'The same caller should work with interchangeable implementations of a shared behavior.',trap:'The shared method must preserve its contract. A function prop or interface can provide polymorphism without inheritance.',steps:[['Notification request','The service asks its injected sender to send a message.'],['Shared contract','The caller knows send(message), not the provider’s transport.'],['Chosen messenger','Email, SMS, or a fake sender performs its own implementation.'],['Caller continues','The service handles the same success/error contract.']],examples:{JavaScript:'const email = { send: async msg => `email: ${msg}` };\nconst sms = { send: async msg => `sms: ${msg}` };\nasync function notify(sender, msg) { return sender.send(msg); }\nawait notify(email, "Ready");',React:'function AlertAction({onNotify}) {\n return <button onClick={() => onNotify("Ready")}>Notify</button>;\n}\n// Parent injects email, chat, or a test callback.',Python:'class Email:\n    def send(self, message): return f"email: {message}"\nclass SMS:\n    def send(self, message): return f"sms: {message}"\ndef notify(sender, message): return sender.send(message)',CSharp:'interface ISender { Task Send(string message); }\nclass Service(ISender sender) {\n public Task Notify(string message) => sender.Send(message);\n}\n// EmailSender and SmsSender implement ISender.'},challenge:'How would you add Slack without editing notify()? What should a fake sender capture?',hints:['Add an implementation with the same contract.','Record messages in memory.'],answer:'Provide a Slack sender with send(message). Inject it into notify. A fake can store messages and simulate rejection so the caller’s error path can be checked.'},
 {id:'abstraction',title:'Abstraction: drive without rebuilding the engine',world:'OOP',emoji:'🚗',when:'Offer an interface that hides details a caller does not need.',trap:'Hiding details is useful only when the abstraction still exposes important failures and constraints.',steps:[['Checkout','Checkout asks inventory.reserve(sku, quantity).'],['Reservation interface','The caller expresses intent instead of SQL syntax.'],['Implementation','A repository uses an atomic database operation.'],['Outcome','Return reserved or insufficient stock; do not hide failures.']],examples:{Node:'async function checkout(inventory, sku) {\n const reserved = await inventory.reserve(sku, 1);\n if (!reserved) throw Error("Out of stock");\n return "Reserved";\n}',CSharp:'interface IInventory { Task<bool> Reserve(string sku, int quantity); }'},challenge:'How is abstraction different from encapsulation?',hints:['One shapes what callers see.','The other controls access to internal state.'],answer:'Abstraction describes useful capabilities while hiding unnecessary implementation details. Encapsulation groups state and behavior and controls access to protect invariants. They often work together.'},
 {id:'composition',title:'Composition versus inheritance',world:'OOP',emoji:'🧩',when:'Assemble behaviors with collaborators; use inheritance only for a real substitutable “is a” relationship.',trap:'A giant class hierarchy makes unrelated features depend on each other. React component composition usually expresses UI reuse more clearly.',steps:[['Order service','Needs pricing and notification capabilities.'],['Compose','Receive a price calculator and sender as separate values.'],['Swap a part','Change the pricing strategy without changing the sender.'],['Reuse','Reuse the sender in a different service.']],examples:{JavaScript:'const orderService = (price, sender) => ({\n async place(cart) {\n  const total = price(cart);\n  await sender.send(`Total: ${total}`);\n  return total;\n }\n});',React:'function Card({children}) { return <section>{children}</section>; }\n// <Card><Profile /></Card> composes UI without subclassing.'},challenge:'Would a Penguin subclass of FlyingBird preserve the contract?',hints:['Ask what every FlyingBird promises.'],answer:'No, if FlyingBird guarantees fly(). Model Bird separately and supply flying behavior only to birds that can fly. Prefer capability interfaces over false inheritance.'},
 {id:'solid-s',title:'SOLID S: one reason to change',world:'SOLID',emoji:'🛠️',when:'Separate responsibilities whose requirements change independently.',trap:'Single responsibility does not mean every function must contain one line.',steps:[['Validate','A validator checks input rules.'],['Calculate','A pure price function computes the total.'],['Persist','A repository stores the order.'],['Present','React renders the result.']],examples:{Node:'const total = calculateTotal(validatedItems);\nconst order = await orders.save({total});\nreturn order;\n// Validation, pricing and persistence have separate owners.'},challenge:'A React component fetches, parses CSV, computes tax, and renders. What would you extract first?',hints:['Look for behavior that changes for separate business reasons.'],answer:'Extract CSV parsing and tax calculation as pure modules, and data access into a suitable service/hook. Keep UI state and rendering together where cohesive; avoid fragmentation for its own sake.'},
 {id:'solid-o',title:'SOLID O: extend through a strategy',world:'SOLID',emoji:'🔌',when:'Add new behaviors through a stable extension point.',trap:'Do not predict every future requirement. Introduce an extension point when concrete variation exists.',steps:[['Shipping request','The order selects an existing shipping strategy.'],['Stable caller','The checkout invokes quote(weight).'],['Extension','Add a pickup strategy without editing checkout.'],['Validation','Reject unknown strategies at the boundary.']],examples:{JavaScript:'const rates = {\n standard: kg => 20 + kg * 4,\n express: kg => 50 + kg * 8\n};\nfunction quote(kind, kg) {\n if (!rates[kind]) throw Error("Unknown shipping");\n return rates[kind](kg);\n}'},challenge:'Does open/closed mean existing source files must never change?',hints:['Think about stable behavior versus adding variants.'],answer:'No. It encourages extending a stable design without repeatedly rewriting its central decision logic. Fixing bugs and changing requirements still require edits.'},
 {id:'solid-l',title:'SOLID L: keep the promise',world:'SOLID',emoji:'🤝',when:'Subtypes must remain usable wherever their base contract is expected.',trap:'Having the same method names does not ensure substitutability.',steps:[['Caller promise','A reader expects read() to return content or a documented error.'],['Substitute','Swap file storage for object storage.'],['Same guarantees','Keep input requirements, result meaning, and error expectations.'],['Broken contract','A subtype that silently deletes data on read violates expectations.']],examples:{JavaScript:'async function preview(reader) {\n const text = await reader.read();\n return text.slice(0, 100);\n}\n// Every reader returns a string; not null or an unrelated object.'},challenge:'If a subtype rejects inputs the base type accepts, what breaks?',hints:['The caller cannot safely rely on the base contract.'],answer:'The subtype strengthens preconditions and can break existing callers. Keep the shared contract or introduce a different capability instead of forcing inheritance.'},
 {id:'solid-i',title:'SOLID I: smaller capability contracts',world:'SOLID',emoji:'🎛️',when:'Different consumers need different subsets of a service.',trap:'Do not force read-only consumers to implement save/delete methods.',steps:[['Reader','The report page only lists orders.'],['Writer','The admin workflow creates orders.'],['Separate interfaces','Expose OrderReader and OrderWriter.'],['Focused fake','A report test supplies only a fake reader.']],examples:{CSharp:'interface IOrderReader { Task<string[]> List(); }\ninterface IOrderWriter { Task Save(string order); }',JavaScript:'function report(reader) { return reader.list(); }\n// reader need not provide save() or delete().'},challenge:'How can React props follow interface segregation?',hints:['Pass only the data and actions the child needs.'],answer:'Give a presentational child narrow props instead of a whole application service/store. It can render and emit callbacks without knowing unrelated operations.'},
 {id:'solid-d',title:'SOLID D: inject the dependency',world:'SOLID',emoji:'💉',when:'Business logic should depend on a capability rather than constructing a concrete infrastructure client.',trap:'Dependency injection is one technique; inversion concerns the direction of dependency. Too many abstractions can obscure simple code.',steps:[['Business rule','Registration needs to save a user.'],['Capability','It accepts a user repository interface.'],['Composition root','Server startup injects the database implementation.'],['Local check','Development supplies an in-memory repository.']],examples:{Node:'const register = users => async user => {\n if (!user.email) throw Error("Email required");\n return users.save(user);\n};\n// Startup: const handle = register(postgresUsers);',Python:'def register(users, user):\n    if not user.get("email"): raise ValueError("Email required")\n    return users.save(user)'},challenge:'Where should the real database client be constructed?',hints:['Keep infrastructure wiring at the application boundary.'],answer:'At startup/composition root, then pass its adapter into the service. This separates wiring from business decisions and permits focused fakes.'},
 {id:'closure',title:'Closure: the backpack that remembers',world:'JavaScript & React',emoji:'🎒',when:'A returned function needs access to variables from the scope where it was created.',trap:'Creating the factory again creates new state. React callbacks may capture an earlier render’s values.',steps:[['Factory call','createCounter() creates a new count binding starting at zero.'],['Return function','The returned function retains access to that binding.'],['First call','The same function increments its binding to 1.'],['Second call','The same function increments it to 2; another factory call would start at 0.']],examples:{JavaScript:'function createCounter(){\n let count=0;\n return () => ++count;\n}\nconst a=createCounter(), b=createCounter();\nconsole.log(a(), a(), b()); // 1 2 1'},challenge:'What does createCounter()(), createCounter()() print, and why?',hints:['Count the number of factory calls.'],answer:'1 and 1: two calls create independent count bindings. Saving a returned function and calling it twice instead gives 1 and 2.'},
 {id:'event-loop',title:'Event loop: two waiting rooms',world:'JavaScript & React',emoji:'🚦',when:'Reason about synchronous execution, Promise callbacks, and timer callbacks in browser JavaScript.',trap:'A zero-delay timer is not immediate. Microtasks can delay later tasks and rendering. Node also has phases and nextTick behavior beyond this browser example.',steps:[['Call stack','Run synchronous logs A and D. Schedule Promise and timer callbacks.'],['Microtasks','After the current task, drain the Promise callback: C.'],['Next task','The eligible timer callback runs: B.'],['Result','The browser example logs A, D, C, B.']],examples:{JavaScript:'console.log("A");\nsetTimeout(() => console.log("B"), 0);\nPromise.resolve().then(() => console.log("C"));\nconsole.log("D");'},challenge:'What if the Promise callback queues another microtask?',hints:['The microtask queue drains until empty.'],answer:'The new microtask also runs before the next timer task. Recursively adding microtasks can starve other work.'},
 {id:'react-state',title:'React: send a new snapshot',world:'JavaScript & React',emoji:'📸',when:'A UI changes after an event and React needs a new state snapshot.',trap:'Mutating an existing state object can keep its identity and break update assumptions. State setters do not change the current render’s captured value.',steps:[['Click','A handler requests an update with setItems.'],['New value','Create a new array rather than pushing into the old state array.'],['Render','React calls components to describe the next UI.'],['Commit','React applies necessary DOM changes; render and DOM mutation are separate.']],examples:{React:'const [items,setItems]=useState([]);\nfunction add(){\n setItems(old => [...old, {id:crypto.randomUUID(),name:"Book"}]);\n}\n// items.push(...) mutates existing state; avoid it.'},challenge:'Why use a functional state updater when the next value depends on the previous one?',hints:['Multiple updates may be queued.'],answer:'React passes the pending previous state into the updater, avoiding reliance on a stale captured value. The updater must be pure.'},
 {id:'memo',title:'Memoization: the reusable answer shelf',world:'JavaScript & React',emoji:'🗄️',when:'An expensive pure calculation repeats with unchanged dependencies.',trap:'useMemo is a performance optimization, not durable storage or a correctness guarantee. Missing dependencies create stale results.',steps:[['Render inputs','The component receives items and query.'],['Compare dependencies','React compares each dependency using Object.is.'],['Reuse or calculate','Reuse the cached calculation when dependencies match; otherwise recalculate.'],['Render result','Use the filtered result without changing state during calculation.']],examples:{React:'const visible=useMemo(\n () => items.filter(x => x.name.includes(query)),\n [items,query]\n);\n// Measure before memoizing a cheap calculation.'},challenge:'What happens if query is omitted from the dependency array?',hints:['A query change no longer invalidates the memo.'],answer:'The UI may keep a filtered result from an old query. Include all reactive values used by the calculation.',source:'https://react.dev/reference/react/useMemo'},
 {id:'context',title:'Context: a shared radio channel',world:'JavaScript & React',emoji:'📻',when:'Distant descendants need the same cross-cutting value, such as theme or authenticated user information.',trap:'Context is not a database or a server authorization check. Provider value changes cause consuming components to receive the new value.',steps:[['Provider','A parent supplies a theme value.'],['Tree path','Intermediate components do not need to forward a theme prop.'],['Consumer','useContext reads the closest matching provider.'],['Update','Changing the provided value updates its consumers.']],examples:{React:'const ThemeContext=createContext("light");\nfunction Button(){\n const theme=useContext(ThemeContext);\n return <button className={theme}>Save</button>;\n}\n// Wrap descendants in a matching provider.'},challenge:'When would you prefer props over Context?',hints:['Consider a short, explicit data path.'],answer:'Use props for local component relationships and explicit reusable APIs. Use Context when repeated prop forwarding obscures a shared concern.',source:'https://react.dev/learn/passing-data-deeply-with-context'},
 {id:'idempotency',title:'Idempotency: one ticket, one effect',world:'Systems',emoji:'🎟️',when:'A client retries an operation after a timeout and duplicate side effects would be harmful.',trap:'A cache check followed by an unprotected insert races. Keys need scope, request fingerprints, retention policy, and atomic reservation.',steps:[['First request','Client sends a stable operation key with an order request.'],['Reserve atomically','Server claims the key using a unique constraint and validates the payload fingerprint.'],['Commit effect','Store the order and completed response atomically where possible; external effects need coordinated recovery.'],['Retry','The same key and payload retrieves the prior outcome; a changed payload is rejected.']],examples:{Node:'// Transaction sketch; enforce UNIQUE(user_id, key).\n// 1. Claim key or inspect existing operation.\n// 2. Compare request fingerprint.\n// 3. Create order + record result atomically.\n// 4. Retry returns stored result (or pending status).'},challenge:'Does idempotent mean every retry returns the same HTTP status?',hints:['The definition concerns the intended effect on server state.'],answer:'No. Repeating the operation has the same intended effect as applying it once. Responses can differ, such as a resource already being deleted. POST can be made idempotent at application level with an appropriate protocol.',source:'https://developer.mozilla.org/en-US/docs/Glossary/Idempotent'},
 {id:'websocket',title:'WebSocket: keep the conversation open',world:'Systems',emoji:'📡',when:'Both client and server need low-latency messages, such as chat or collaborative editing.',trap:'An open socket does not guarantee delivery after reconnect. Authenticate, validate messages, control buffering, and design replay/acknowledgment if required.',steps:[['Handshake','Client establishes a WebSocket connection to an authorized endpoint.'],['Open channel','Either side can send messages while the connection is open.'],['Disconnect','Network loss interrupts delivery; the client detects closure.'],['Resume','Reconnect with backoff and jitter, reauthenticate, and resume from an application-level cursor.']],examples:{JavaScript:'const socket=new WebSocket("wss://example.com/chat");\nsocket.addEventListener("open",()=>socket.send("hello"));\nsocket.addEventListener("message",event=>console.log(event.data));\n// Example endpoint only; add cleanup, validation and reconnect handling.'},challenge:'When would server-sent events be simpler?',hints:['Do you need server-to-client updates only?'],answer:'For a unidirectional event feed over HTTP, SSE may be simpler. WebSocket supports bidirectional messaging; ordinary HTTP remains suitable for discrete requests.',source:'https://developer.mozilla.org/en-US/docs/Web/API/WebSockets_API'},
 {id:'webpack',title:'Webpack: pack the dependency expedition',world:'Systems',emoji:'📦',when:'Bundle modules and assets into output files using a dependency graph and build pipeline.',trap:'Webpack is a build tool, not an application server or WebSocket library. Do not assume every modern React project uses it.',steps:[['Entry','Begin at the configured entry module.'],['Dependency graph','Follow imports to discover required modules and assets.'],['Transform','Configured loaders transform file types; plugins hook into broader build stages.'],['Output','Emit bundles/assets; splitting can create separate chunks to load on demand.']],examples:{JavaScript:'// webpack.config.js — illustrative configuration\nmodule.exports={\n mode:"production",\n entry:"./src/index.js",\n output:{filename:"app.js"}\n};'},challenge:'How is a loader different from a plugin?',hints:['One transforms modules; one participates in the build lifecycle.'],answer:'Loaders transform matching resources, such as turning a source file into a module webpack can process. Plugins customize broader compilation behavior, such as asset generation.',source:'https://webpack.js.org/concepts/'},
 {id:'redis',title:'Redis: the shortcut with an expiry',world:'Systems',emoji:'⚡',when:'Low-latency cache, rate-limiting counters, or other data operations fit the required consistency and durability tradeoffs.',trap:'A cache is not automatically authoritative. Plan expiry, invalidation, stampede control, and behavior when Redis is unavailable.',steps:[['Read key','Application checks a scoped cache key.'],['Miss','On a miss, read the authoritative database.'],['Fill','Store a permitted result with a chosen expiry.'],['Invalidate','A write invalidates or updates the relevant cached data; define acceptable staleness.']],examples:{Node:'// Adapter sketch\nconst cached=await cache.get(key);\nif(cached!==null) return JSON.parse(cached);\nconst value=await repository.read(id);\nawait cache.setWithExpiry(key, JSON.stringify(value), 60);\nreturn value;'},challenge:'What happens when 1000 requests miss the same expired key?',hints:['Avoid making all of them recompute simultaneously.'],answer:'A cache stampede can overload the database. Consider request coalescing, controlled stale serving, and staggered expiry according to freshness requirements.'},
 {id:'serverless',title:'Serverless: summon a worker',world:'Systems',emoji:'🪄',when:'Event-triggered workloads benefit from managed compute and scaling without managing servers directly.',trap:'Servers still exist. Cold starts, execution limits, concurrency, costs and state storage affect the design.',steps:[['Event','An HTTP request or queue message triggers a function.'],['Execution','The platform assigns an execution environment.'],['External state','Persist durable state outside the function’s local memory.'],['Retry','A retried event must not duplicate harmful effects.']],examples:{JavaScript:'export async function handler(event) {\n const command=parseAndValidate(event);\n return processIdempotently(command);\n}\n// Sketch: adapters implement validation, storage and idempotency.'},challenge:'Why is an in-memory counter unreliable as a global rate limit?',hints:['Concurrent invocations can run in different environments.'],answer:'Each environment sees only its local memory, and environments may be replaced. Use shared state with atomic operations when the limit must be global.'},
 {id:'greedy',title:'Greedy: choose the next compatible stop',world:'Algorithm strategy',emoji:'🧭',when:'A local choice can be proved to preserve an optimal solution; interval scheduling is a classic example.',trap:'“Choose the biggest” is not a proof. Greedy coin change fails for some denominations.',steps:[['Intervals','Meetings are [1,3], [2,5], [3,4], [4,6]. Touching endpoints are allowed.'],['Sort by finish','Choose [1,3], the earliest finishing meeting.'],['Take compatible','Then take [3,4] and [4,6].'],['Prove','An optimal schedule’s first meeting can be replaced by an earliest-finishing one without reducing room for later meetings.']],examples:{JavaScript:'const sorted=[...intervals].sort((a,b)=>a[1]-b[1]);\nconst chosen=[];let end=-Infinity;\nfor(const meeting of sorted){\n if(meeting[0]>=end){chosen.push(meeting);end=meeting[1];}\n}'},challenge:'With coins [1,3,4], why does largest-coin-first fail for amount 6?',hints:['Compare 4+1+1 with 3+3.'],answer:'Greedy uses 3 coins while the optimum uses 2. Use an appropriate DP or a proven denomination-specific property.'},
 {id:'shortest-path',title:'Choose BFS, Dijkstra, or Bellman–Ford',world:'Algorithm strategy',emoji:'🛤️',when:'You need minimum path cost; the edge-weight assumptions decide the algorithm.',trap:'DFS reachability is not a shortest-path solution. Dijkstra is not valid for arbitrary negative edges.',steps:[['Equal weights','Use BFS: level count is path length in an unweighted graph.'],['Nonnegative weights','Use Dijkstra: repeatedly finalize the smallest tentative distance with a priority queue.'],['Negative weights','Consider Bellman–Ford; reachable negative cycles may mean no finite shortest path.'],['Verify assumptions','State graph direction, disconnected behavior, and what distance means.']],examples:{JavaScript:'// Dijkstra relaxation sketch after popping current u:\nfor (const [v,weight] of adjacency[u]) {\n const candidate=distance[u]+weight;\n if(candidate<distance[v]){\n  distance[v]=candidate;\n  heap.push([candidate,v]);\n }\n}\n// Reject stale heap entries; require nonnegative weights.'},challenge:'A→B costs 10, A→C costs 1, C→B costs 1. Which path wins?',hints:['Add edge costs, not edge count.'],answer:'A→C→B costs 2 and beats direct cost 10. BFS optimizes edge count here, so weighted shortest paths require a suitable algorithm.'},
 {id:'topological',title:'Topological sort: unlock prerequisites',world:'Algorithm strategy',emoji:'🗝️',when:'Order dependencies in a directed acyclic graph: course prerequisites or build tasks.',trap:'There may be several valid orders. A directed cycle prevents a complete topological order.',steps:[['Count incoming edges','For A→C, B→C, C→D: A and B have indegree 0.'],['Ready queue','Take A, then B; decrement C for each completed prerequisite.'],['Unlock','C reaches zero; take C and unlock D.'],['Check count','If fewer than all vertices were processed, a cycle blocks completion.']],examples:{JavaScript:'const ready=vertices.filter(v=>indegree[v]===0);\nconst order=[];\nfor(let i=0;i<ready.length;i++){\n const u=ready[i];order.push(u);\n for(const v of adj[u]) if(--indegree[v]===0) ready.push(v);\n}\n// order.length < vertices.length implies a cycle.'},challenge:'What changes if D also points back to C?',hints:['C and D cannot both finish their prerequisites.'],answer:'C↔D creates a cycle. A and B can be processed, but C and D remain blocked; report the cycle instead of returning a claimed complete order.'},
];

export const behavioralPrompts=[
 ['ownership','Tell me about a production problem you took ownership of.','Distinguish your actions from the team’s actions. What happened to users?'],
 ['conflict','Describe a technical disagreement with a teammate.','Explain how you listened, compared evidence, and reached a decision.'],
 ['deadline','Tell me about a deadline you missed.','When did you communicate risk? What changed afterward?'],
 ['mistake','Describe a mistake you made and how you repaired it.','Avoid blame. Include detection, mitigation, and prevention.'],
 ['ambiguity','Tell me about a requirement that was unclear.','Which questions and assumptions helped you move forward?'],
 ['feedback','Describe difficult feedback you received.','Give a specific behavior you changed and evidence of improvement.'],
 ['performance','Describe a performance improvement you delivered.','Name the baseline, measurement, tradeoff, and result.'],
 ['customer','Tell me about balancing a customer request and technical risk.','Explain the options and how you communicated the compromise.'],
 ['mentoring','How have you helped a teammate learn something?','Describe their need and how you checked understanding.'],
 ['priorities','Describe competing urgent tasks.','Explain impact, dependencies, and the priority discussion.'],
 ['learning','Tell me about learning an unfamiliar technology quickly.','What did you build or verify instead of only reading?'],
 ['initiative','Describe an improvement you proposed without being asked.','Explain the problem and how you gained support.'],
 ['quality','When did you push back on shipping a risky change?','Explain evidence, alternatives, and ownership of the decision.'],
 ['failure','Tell me about an approach that did not work.','Show how evidence changed your plan.'],
 ['communication','Explain a technical issue to a nontechnical stakeholder.','Use impact and options before implementation details.'],
 ['teamwork','Tell me about helping another team deliver.','Include handoffs, constraints, and shared outcomes.'],
 ['scope','Describe a last-minute scope change.','How did you renegotiate time, scope, or quality?'],
 ['security','Describe protecting user data in a feature you worked on.','Discuss actual controls and tradeoffs without exposing private information.'],
 ['motivation','Why are you considering this role?','Connect specific role responsibilities with evidence from your work.'],
 ['growth','What skill are you currently improving?','Give an honest gap, your practice routine, and a recent example of progress.'],
] as const;
